const fs = require("fs"); let c = fs.readFileSync("app/(parent-drawer)/(tabs)/notices.tsx", "utf8"); c = c.replace(/TYPE_CONFIG/g, "getNoticeConfig(colors, isDark)"); let cfg = `\nfunction getNoticeConfig(colors: any, isDark: boolean): Record<string, any> {\n  return {\n    general: { icon: "information-circle", color: colors.info, bg: isDark ? "#1a365d" : "#E0F2FE", label: "General" },\n    urgent: { icon: "alert-circle", color: "#EF4444", bg: isDark ? "#450a0a" : "#FEE2E2", label: "Urgent" },\n    event: { icon: "calendar", color: colors.success, bg: isDark ? "#14532d" : "#DCFCE7", label: "Event" },\n    holiday: { icon: "star", color: colors.warning, bg: isDark ? "#78350f" : "#FEF3C7", label: "Holiday" },\n    exam: { icon: "document-text", color: colors.purple, bg: isDark ? "#4c1d95" : "#F3E8FF", label: "Exam" }\n  };\n}\n`; if (!c.includes("getNoticeConfig")) c += cfg; fs.writeFileSync("app/(parent-drawer)/(tabs)/notices.tsx", c);
